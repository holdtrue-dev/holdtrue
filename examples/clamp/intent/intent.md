# Intent: clamp

Clamp a number into a range. Given `x`, `lo`, `hi`, return `x` if it sits inside
`[lo, hi]`, otherwise return the nearest bound.

This is the raw intent. Open questions are in `questions.md`; how they were
resolved is in `decisions.md`.
