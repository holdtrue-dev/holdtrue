# Intent: paginate

Work out a page of results. Given a total number of items, a page size from 1 to 100, and a page number starting at 1, return the offset of the first item on that page, the limit (the page size), and the total number of pages. The total pages must round up so the last partial page still counts.
