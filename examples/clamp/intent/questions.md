# Questions: clamp

Raised before any code was written. All resolved (see `decisions.md`).

- `lo > hi`: what happens with an inverted range?
- `x == lo` or `x == hi`: are the bounds inclusive?
- ints only, or floats too?
- does it raise on bad input, or is that a precondition?
- when `x` is in range, must it return `x` exactly, or just some in-range value?

Open: 0
